/*import { getFirestore, doc, addDoc, getDoc, setDoc, updateDoc, deleteDoc, collection, getDocs } from 'firebase/firestore';

const db = getFirestore();

export async function addUser(userData: Record<string, any>){
    const docRef = await addDoc(collection(db, 'development-user'), userData );
    return docRef.id;
};*/